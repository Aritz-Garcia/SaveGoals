<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.SaveGoals" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_200</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/black</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_200</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->

        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>

        <!-- Added -->
        <item name="android:textColor">@color/white</item>
        <item name="tint">@color/white</item>
        <item name="iconTint">@color/white</item>
        <item name="android:tint">@color/white</item>
        <item name="compactCalendarBackgroundColor">@color/grey</item>
        <item name="compactCalendarTextColor">@color/white</item>
        <item name="compactCalendarCurrentSelectedDayBackgroundColor">@color/black</item>
        <item name="compactCalendarCurrentSelectedDayTextColor">@color/white</item>
        <item name="compactCalendarCurrentDayBackgroundColor">@color/azul</item>
        <!-- Fondo de pagina en grey -->
        <item name="android:windowBackground">@color/grey</item>
        <item name="tabBackground">@color/grey</item>
    </style>
</resources>